#include "GameState.hpp"

GameState::GameState() {
    // Player player = Player();
    GameState_t state = GameState_t::WORLD;
    // BattleEngine engine;
    // Arena arena = Arena();
    // Animator animator = Animator();
}